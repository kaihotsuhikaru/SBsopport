<h1>タスクの編集</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '削除', @task, method: :delete, data:{ confirm: '本当によろしいですか？'} %>
<%= link_to '戻る', tasks_path %>